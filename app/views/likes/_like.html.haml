.iine
  - if current_user.likes.find_by(photo_id: photo.id)
    = link_to unlike_path(photo.id), method: :delete, remote: true, class: "iine__button", id:"like-link-#{photo.id}" do
      %i.fa.fa-heart.heart-like
      %p.info いいね!
      %P.up
        = "#{photo.likes.count}"
  - else
    = link_to like_path(photo.id), method: :post, remote: true, class: "iine__button", id:"like-link-#{photo.id}" do
      %i.fa.fa-heart.heart-like
      %p.info いいね!
      %P.down
        = "#{photo.likes.count}"
